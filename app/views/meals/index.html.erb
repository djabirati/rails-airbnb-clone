
<div class="content">
  <div class="row">
    <div class="cards col-7">
        <% @meals.each do |meal| %>
          <%# <%= link_to meal_path(meal.id) do %>
            <div class="card-product">
              <%= cl_image_tag(meal.photo.key) %>
              <div class="card-product-infos">
                <div>
                  <h2><%= link_to meal.title, meal_path(meal) %><h2>
                  <%# <h4><%= meal.user.nickname<h4> %>
                  <p><%= meal.description %></p>
                </div>
                <h2 class="card-product-pricing"><%= meal.price %>€</h2>
                <%= cl_image_tag(meal.user.photo.key, class: 'card-product-user avatar-bordered') %>
              </div>
            </div>
          <%# <% end %>
        <% end %>
    </div>

    <div class="col-5" id="map"
          style="width: 100%; height: 600px;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
</div>
