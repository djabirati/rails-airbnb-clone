<%= simple_form_for [@meal, @booking] do |f| %>
  <%= f.input :number_customer, placeholder: 'nombre de participant' %>
  <%= f.input :date_hour, placeholder: 'date' %>
  <%= f.submit class: 'btn' %>
<% end %>
